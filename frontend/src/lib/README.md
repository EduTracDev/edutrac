# lib

Use this folder for:

- **Socket client** – e.g. `socket.ts` for real-time features (copy/adapt from whitepenguin `src/lib/socket.ts` when needed).
- **Other shared runtime utilities** that don’t belong in `utils/` or `services/`.
